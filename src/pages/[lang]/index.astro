---
export function getStaticPaths() {
	return [
		{ params: { lang: 'en' } },
		{ params: { lang: 'es' } },
		{ params: { lang: 'de' } },
		{ params: { lang: 'ja' } },
		{ params: { lang: 'ko' } },
	];
}

const { lang } = Astro.params;
return Astro.redirect(`/${lang}/linter`);
---
